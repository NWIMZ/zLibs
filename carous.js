var ele = document.getElementById('container');

/**
 * 
 * @param {Element} el 
 * @param {Object} opts {pics} 
 */
function Carous(el, opts) {
    this.el = el;
    this.opts = opts;
}

/**
 * 初始化
 */
Carous.init = function () {
    // 轮播图的图片
    var pics = this.pics;
};